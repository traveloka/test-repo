version: '3'
services:
  app:
    container_name: docker-toss
    restart: always
    build: .
    ports:
      - '9001:9001'
    links:
      - mongo 
    volumes:
      - ./:/usr/src/app
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017' 